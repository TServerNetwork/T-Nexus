package network.tserver.tnexus.manager.recap.model;

/**
 * Policy controlling how unassigned entities join a recap session.
 */
public enum AutoJoinPolicyType {
    DISABLED,
    FIXED_TEAM,
    OPPONENT_TEAM
}
